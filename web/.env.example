# Локально в браузере: /api (Vite проксирует на :8000)
# Через ngrok в Telegram: /api на том же домене, что WEBAPP_URL
VITE_API_URL=/api

# Only for local dev without Telegram (must be valid signed initData)
# VITE_DEV_INIT_DATA=
